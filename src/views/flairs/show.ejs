<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <h1><%= flair.name %></h1>
      <a href="/posts/<%= flair.postId %>/flairs/<%= flair.id %>/edit" class="btn btn-warning">Edit</a>
      <a href="/posts/<%= flair.postId %>/flairs/new" class="btn btn-success">New Flair</a>
      <form style="display:initial" action="/posts/<%= flair.postId %>/flairs/<%= flair.id %>/destroy" method="post">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
      <p><%= flair.color %></p>
      <h1>Flairs</h1>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
